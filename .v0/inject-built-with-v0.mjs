// Compatibility shim for the existing Vercel project build command.
// The project was originally created by v0, but this repository snapshot does
// not include v0's generated helper. Keeping this no-op lets Vercel run the
// configured command before `next build`.
